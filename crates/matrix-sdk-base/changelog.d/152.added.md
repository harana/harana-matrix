`MediaFormat` and `MediaThumbnailSettings` now implement `PartialEq`, `Eq`,
`PartialOrd` and `Ord`, so a media format can be used as a key in a sorted map.
Files sort before thumbnails, and thumbnails sort by resizing method and then by
dimensions.
