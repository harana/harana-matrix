Sliding sync no longer repeats every room subscription, with its full
`required_state`, on every single `/sync` request. Room subscriptions are sticky
server-side, so only new and changed ones are sent now; everything is sent again
when a new sliding sync session starts.
