Discover threads when their events are decrypted. An in-thread event whose
relation isn't visible while it is a UTD could not be routed to a thread cache,
so the thread was invisible in the main timeline; and after a restart no thread
cache is alive, so keys arriving from backup refreshed no thread summary.
