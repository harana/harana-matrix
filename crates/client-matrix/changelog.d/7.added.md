Each event the SDK receives in a sync response is now logged, one line per
event, at `TRACE` on the `client_matrix::event_handler` target, with its type,
event ID (when it has one) and room. Seeing what a sync actually delivered
previously meant instrumenting the code by hand.
