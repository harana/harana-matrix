Compute the latest event of a thread. `LatestEvent` carried a thread id but
never used it, so a thread's latest event value stayed `None`: the remote side
now only considers that thread's events and its root, and the send queue side
only the local events sent to that thread.
