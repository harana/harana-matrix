`LoginBuilder::request_config()` and `SsoLoginBuilder::request_config()` let
callers choose the `RequestConfig` used for the login request. The login request
used a hardcoded `RequestConfig::short_retry()` and ignored the one set on
`ClientBuilder`, so a `429 Too Many Requests` with a long `retry_after` failed
the login instead of being waited out.
