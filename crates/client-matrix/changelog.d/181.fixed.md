The room member list is now fetched before a room key is shared. With lazy
loading the state store can hold nothing but our own membership until the
members have been requested, and a room key shared against that list reached
nobody: every message sent with it was permanently undecryptable for the whole
room, with nothing in the result to say so.
