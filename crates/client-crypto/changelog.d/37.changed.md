Document `LocalTrust::BlackListed` as the supported feature it is: what marking
a device that way does (it is left out of every room key we share, told why with
an `m.blacklisted` withheld message, and the current room key is rotated) and
how the block is undone.
