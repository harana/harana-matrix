Room keys downloaded from the key backup which cannot be deserialized or
decrypted are reported as `Error::UnreadableBackedUpRoomKeys` rather than
silently skipped, so a corrupt backed-up key can be told apart from a key which
is simply not in the backup.
