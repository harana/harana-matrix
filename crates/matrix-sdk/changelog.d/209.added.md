Automatically subscribe the user to a thread when somebody answers one of their
own messages in it. Only explicit replies count: the `m.in_reply_to` field of a
threaded event is, by default, just the rich-reply fallback pointing at the
latest event of the thread.
