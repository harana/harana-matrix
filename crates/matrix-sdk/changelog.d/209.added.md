The current user is now automatically subscribed to a thread when somebody
answers one of their messages in it: an explicit in-thread reply to one of their
events, or any reply in a thread they started. Previously only an intentional
mention subscribed them, so they missed follow-ups to their own messages. This
applies to threads discovered through sync and through back-pagination alike.
