The session callbacks passed to `Client::set_session_callbacks` return futures
now, so a host whose session storage is asynchronous does not have to block a
thread inside them. Existing callbacks need wrapping: return
`Box::pin(async move { ... })` instead of the result directly.
