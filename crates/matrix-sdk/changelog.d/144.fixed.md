An edit no longer moves a room in a room list sorted by recency. When the latest
event of a room is an edited message, the latest event value now carries the
timestamp of the event being edited instead of the timestamp of the `m.replace`
event, while still exposing the edited content.
