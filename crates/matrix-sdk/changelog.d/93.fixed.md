The recovery state is now recomputed on a background task instead of inline in
the account data and to-device event handlers, which run on the sync task: the
`/account_data` requests it makes used to hold up sync progress when the server
was slow. Updates are single-flighted, so a change observed while a
recomputation is in flight cancels and restarts it rather than running a second
one concurrently.
