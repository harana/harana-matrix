`Encryption::bootstrap_cross_signing_if_needed_with_outcome` creates and
publishes a cross-signing identity, reporting whether it was created, was
already present, needs user-interactive authentication, or is unavailable.
Passing authentication data always retries the upload, since the identity an
earlier attempt created locally is not one the homeserver has accepted.
