Ignoring or unignoring a user no longer clears the event cache of every room.
Only the events sent by the ignored users are dropped, from the room and thread
caches that are loaded, and every room recomputes its latest event, so the room
list keeps its ordering and unignoring a user restores what their messages hid.
