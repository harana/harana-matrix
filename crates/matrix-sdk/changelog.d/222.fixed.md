Ignoring or unignoring a user no longer clears the whole event cache. That wiped
every room's preview and left the room list ordering mixing latest-event
timestamps with recency stamps, and unignoring didn't restore anything. The
latest event of every room is now recomputed from the events that are still
cached, in both directions.
