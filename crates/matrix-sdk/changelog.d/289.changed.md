The cross-process lock functions now use "holder name" consistently for the
value identifying the process holding a lock:
`Encryption::enable_cross_process_store_lock` and
`OAuth::enable_cross_process_refresh_lock` take a `holder_name` rather than a
`lock_value`. The latter also documents that it guards token refreshes, and is
independent from the crypto-store lock guarded by the former.
