The event cache no longer drops a previous-batch token when a batch of events
is entirely made of duplicates that are laid out differently in the linked
chunk than the server just sent them. The duplicates are now re-inserted in the
order given by the server, and back-pagination carries on from that token,
instead of stopping there.
