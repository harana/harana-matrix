Marking a room as read now has a local echo. `Room::send_single_receipt` and
`Room::send_multiple_receipts` apply the read receipt to the room's unread
counts as soon as the request is issued, instead of waiting for the receipt to
come back through sync, and roll the change back if the request fails.
