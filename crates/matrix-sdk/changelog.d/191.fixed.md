A `/keys/upload` rejected by the homeserver with a 400 is treated as terminal
and marked as sent, instead of being regenerated on every sync forever. This is
what a restored session whose crypto store had lost its account used to do.
