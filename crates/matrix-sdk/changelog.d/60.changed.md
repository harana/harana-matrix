`VerificationRequestState::Done` is now a struct variant carrying the
`other_device_data` of the device that was verified, so a completion dialog can
name it. It is `None` when another of our own devices handled the request.
