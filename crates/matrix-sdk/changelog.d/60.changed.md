`VerificationRequestState::Done` carries the `other_device_data` of the device
that was verified, and `VerificationRequest` gained `other_device_data` and
`other_device_id` accessors that stay available once the flow is done.
