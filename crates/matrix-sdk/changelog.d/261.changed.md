`Backups::wait_for_steady_state` is deprecated in favour of
`Backups::trigger_upload` and `Backups::wait_for_upload`: the old method read as
passive but woke the upload task up, and it could resolve on the `Done` of an
upload that finished before it was called. The new wait only resolves on an
upload which completes after the future was created.
