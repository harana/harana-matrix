Per-key failures reported in the body of a successful `/keys/signatures/upload`
response are now logged, and our own keys are read back from the homeserver
afterwards. A device could finish an emoji verification and consider itself
verified while the self-signing key signature never made it onto the server, so
everyone else still saw it as unverified.
