A back-pagination no longer loops forever when the server answers `/messages`
with an empty chunk and the very token the request carried. Such a response is
now treated as the start of the timeline, so the pagination terminates and
observers stop being told one is running.
