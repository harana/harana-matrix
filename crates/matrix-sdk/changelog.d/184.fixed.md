The received room key bundle data is now dropped once the bundle has been
imported or found to be malformed, rather than kept indefinitely. The uploaded
file itself still outlives its use: the client-server API has no way to delete a
piece of media.
