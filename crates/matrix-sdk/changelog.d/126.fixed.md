`Recovery::disable()` can now delete a key backup that exists only on the
server. When no backup key is held locally, it used to fail with "backups are
not enabled" and leave the server-side backup in place forever; it now falls
back to deleting whatever versions the server has.
