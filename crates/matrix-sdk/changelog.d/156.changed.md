A cross-signing key which the secret store held but which could not be imported
now fails recovery with `SecretStorageError::IncompleteCrossSigningImport`,
instead of silently leaving the identity partially restored.
