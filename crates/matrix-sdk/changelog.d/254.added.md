Add a `rustls-ring` feature, selecting ring rather than aws-lc-rs as the rustls
crypto provider. aws-lc-rs crashes client initialisation on some Android
targets and emulators (16 KB page size arm64 among them). The provider is
installed process-wide the first time an HTTP client is built, and installing
is a no-op if one is already set.
