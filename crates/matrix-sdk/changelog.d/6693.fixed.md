Rescinded third-party invites no longer show up as errors in the timeline. A rescinded `m.room.third_party_invite` arrives with an empty `content`, which failed to deserialize and produced a "failed to parse" timeline item; the SDK now enables ruma's `compat-optional` feature so such an event is parsed as a third-party invite with empty fields, which clients can render as a withdrawn invite.
