A bundled aggregation that arrived undecryptable is now retried when its room
key turns up. The event carrying it is stored as decrypted and indexed under the
outer event's session ID, so the redecryptor's session lookup never found it and
an edit that arrived before its key stayed invisible forever.
