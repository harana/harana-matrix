An event bundled in the `unsigned` object of another event, such as an edit or
the latest event of a thread, is now decrypted again when its room key arrives.
Until now only the events of a linked chunk were retried, and a bundled event
whose parent was perfectly decrypted was never looked at again, so the edit
stayed hidden even once the key was known.
