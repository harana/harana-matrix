Stop logging a `WARN` on every sync for clients that never set up key
backup (e.g. bots). `BackupUploadingTask` now checks whether backups are
enabled before attempting to back up room keys, matching the check
already performed by the backup download task.
