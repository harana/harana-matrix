Clients that never enabled key backups no longer get a `Trying to backup room
keys but no backup key was found` warning logged on every sync response: the
backup upload path is now skipped entirely when backups aren't enabled.
