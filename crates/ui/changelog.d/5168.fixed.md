The timeline's event filter is now also applied to local echoes coming from the send queue. A timeline configured with a custom filter used to show local echoes the filter excluded, so local and remote events were filtered inconsistently. Aggregations (edits, reactions, poll responses) are still applied to their target item regardless of the filter, exactly like their remote counterparts.
