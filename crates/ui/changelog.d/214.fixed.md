Warn when a thread-focused timeline is built on a client configured without
threading support. In that configuration local echoes are never replaced and
thread summaries are never updated, and the misconfiguration used to be silent.
