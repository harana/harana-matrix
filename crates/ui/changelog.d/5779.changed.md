Building a thread-focused timeline on a `Client` that was created without threading support (`ClientBuilder::with_threading_support`) now logs a warning. Without threading support, local echoes in the thread are never replaced by their remote counterpart and the thread summaries of the main timeline are never updated, which used to happen silently.
