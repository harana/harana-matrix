Added logging to the update path between the `RoomEventCache` and the `Timeline`. In particular, a timeline whose event cache dropped its update sender (after which the timeline silently stops receiving any update) now logs a warning instead of ending its task quietly.
