Live location sharing (MSC3489) timeline items can now be replied to. `EventTimelineItem::can_be_replied_to` used to refuse them because they are state events, even though other clients allow it and users expect the same behaviour as for static location messages.
