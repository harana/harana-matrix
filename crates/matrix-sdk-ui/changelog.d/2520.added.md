`MsgLikeKind::Redacted` now carries a `RedactedMessage` with the user ID and the profile of whoever sent the redaction, when that is known: either because the timeline saw the redaction event, or because the homeserver bundled it in the redacted event's `unsigned.redacted_because`. Clients can use this to show who deleted a message.
