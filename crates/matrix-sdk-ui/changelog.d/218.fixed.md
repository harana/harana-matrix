`test_retry_decryption_updates_reply` no longer assumes the decrypted event and
the reply pointing at it are updated in a fixed order, and
`test_false_positive_late_decryption_regression` waits for the conditions it
cares about instead of sleeping for a fixed duration. Both were intermittently
failing on a loaded machine.
