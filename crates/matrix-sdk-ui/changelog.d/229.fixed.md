`SpaceRoomList::paginate` no longer publishes duplicate entries when a
`/hierarchy` response repeats a room the list already holds, either across
pages or within a single page. Rooms are now deduplicated by room ID before
they are pushed to the subscribers.
