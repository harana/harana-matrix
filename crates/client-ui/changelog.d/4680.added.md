Added a `DateDividerMode::None` variant, so a timeline can be configured to never generate date dividers. Timelines that don't render dividers, such as a media swipe-through gallery, previously had to pick a divider mode and then filter the dividers out again.
