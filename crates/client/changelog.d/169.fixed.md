An event sent by an ignored user is no longer picked as a room's latest event,
so a message from a blocked user can't be shown as a room list preview, in a
room whose cache still holds their messages.
