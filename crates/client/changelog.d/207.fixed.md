The `emoji_verification` example builds against the current verification API
again, and now covers both directions: it still accepts incoming requests
arriving as to-device events or in-room messages, and `--verify <user-id>` sends
a request of its own and drives it to completion.
