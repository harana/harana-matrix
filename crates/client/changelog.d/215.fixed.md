Event handlers registered for a state event are no longer called twice when the
sync response carries the state changes at the *end* of the timeline (sliding
sync, or sync v2 with `state_after`): such a response lists a state event both in
the state section and in the timeline. The event is now dispatched to state
handlers once; handlers for `AnySyncTimelineEvent` still see the timeline event.
