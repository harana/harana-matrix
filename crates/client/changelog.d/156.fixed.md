Recovery reports a specific `SecretStorageError::IncompleteCrossSigningImport`
when a cross-signing key that secret storage holds cannot be imported, instead
of silently completing with a partial identity. A failed user-signing key import
used to leave the user retrying the same recovery key in a loop.
