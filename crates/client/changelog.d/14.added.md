`client_matrix::Error` now has a `ClientBuild` variant, together with
`From<ClientBuildError>` and (behind the `sqlite` / `indexeddb` features)
`From<OpenStoreError>` implementations. Callers of `ClientBuilder::build` and of
the store-crate constructors can now propagate those errors with `?` into
functions returning `client_matrix::Error`.
