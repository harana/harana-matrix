Keep the event cache's ordering tracker in sync when lazy-loading a chunk into a
linked chunk that was built without the metadata of the unloaded chunks. The
tracker could not order that chunk's events, which is what had forced the
ordering assertions to be disabled; they are re-enabled.
