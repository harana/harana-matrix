`RelationalLinkedChunk::insert_chunk()` (used by `MemoryStore`) now rejects
inserting a chunk whose identifier is already used by another chunk in the
same linked chunk, returning `RelationalLinkedChunkError::DuplicateChunkIdentifier`
instead of silently corrupting the existing chunk's links.
