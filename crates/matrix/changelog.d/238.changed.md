The pinned events of a room are now only loaded, and kept up to date, while at
least one subscriber listens to them: `PinnedEventsCache::subscribe` returns a
counted subscriber, and when the last one is dropped the events are dropped
from memory and the task refreshing them is aborted. The next subscriber gets
the events back from the store, and restarts the task.
