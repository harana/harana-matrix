A `/keys/upload` rejected with a 400 because a one-time key already exists is no
longer retried forever. The request is marked as sent so the keys are dropped
locally and fresh ones are generated, instead of the same rejected request
blocking every other outgoing request behind it.
