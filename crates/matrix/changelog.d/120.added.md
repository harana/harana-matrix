`Error::CorruptBackupRoomKey` distinguishes a room key that is present in the
key backup but cannot be deserialized or decrypted from one that simply isn't
backed up. `Backups::download_room_key()` returns it instead of reporting a
successful download, and the batch download paths log the affected sessions.
