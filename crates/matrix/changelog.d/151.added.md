`DeviceUpdates` now has a `deleted` field, listing the devices that the
homeserver stopped reporting in a `/keys/query` response, so consumers of
`Encryption::devices_stream()` can tell which device disappeared.
