An event-focused cache (used by permalink timelines) now loads the aggregations
of the focused event that fall outside the `/context` window, so edits and
reactions that are older or newer than the loaded events are aggregated onto
the focused event instead of being missing. This also covers the linked chunk
of the focused cache holding the relations of its focused event (#239).
