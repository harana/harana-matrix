Add `SlidingSyncBuilder::without_marking_tracked_users_dirty()`, for sliding sync
instances that must not invalidate the device list cache when they start a new
session. Short-lived syncs that share a crypto store with the application's own
sync used to trigger a `/keys/query` for every tracked user each time they ran.
