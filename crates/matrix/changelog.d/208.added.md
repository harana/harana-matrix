Add `Capabilities::element_call_required`, the list of capabilities an Element
Call widget needs. It used to live in the FFI bindings, where it could neither
be shared with other consumers nor tested outside them.
