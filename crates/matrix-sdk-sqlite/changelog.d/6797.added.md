Add `SqliteStoreConfig::synchronous` to control `PRAGMA synchronous`. The
default changed from SQLite's implicit `FULL` to `NORMAL`, which is safe in
WAL mode and avoids an `fsync` on every commit, significantly reducing write
amplification. `Synchronous::Full` (or `Off`/`Extra`) can still be selected
explicitly.
