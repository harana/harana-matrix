`clippy::future_not_send` is denied crate-wide on non-WASM targets.
`auth_check`, `check_state_independent_auth_rules`,
`check_state_dependent_auth_rules` and `resolve` now need `Send` fetch
callbacks returning `Send` futures over `Send + Sync` events, which is what
lets a homeserver run them on a multi-threaded runtime.
