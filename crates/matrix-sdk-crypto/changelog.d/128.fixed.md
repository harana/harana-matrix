`Device::set_local_trust` reads the device back from the store and changes only
its trust state, instead of writing the in-memory copy and reverting fields
which were updated elsewhere in the meantime.
