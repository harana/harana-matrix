A secret request is only answered once. A request which arrived while we had no
Olm session with the requesting device is put on a wait queue, and the
homeserver keeps redelivering the to-device event until the sync it came in is
acknowledged, so once a key claim gave us a session both copies used to be
served and the secret was sent twice.
