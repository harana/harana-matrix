`DeviceUpdates` now carries a `deleted` map alongside `new` and `changed`, so a
consumer of `Store::devices_stream` can tell which device disappeared when a
device is signed out. Previously the update was empty in that case.
