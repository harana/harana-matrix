A one-time key count in a sync response which predates our last `/keys/upload`
is ignored, so the SDK no longer uploads a second batch of one-time keys right
after the first one during account creation.
