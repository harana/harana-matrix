A megolm session which is already known to be a legacy one, for instance one
restored from a key backup, is no longer downgraded to a non-legacy session when
a later `/keys/query` cannot attribute it to a sender, which hid its messages
when insecure devices were excluded.
