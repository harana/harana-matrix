`UserIdentity::pin` no longer writes the identity back when the store has since
received new cross-signing keys for that user, which used to revert them and
break communication with that user until the next key query.
