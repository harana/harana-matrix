Documented what `LocalTrust::BlackListed` means: it is set only by the consumer
of this crate, it excludes the device from every room key sharing strategy, the
device is told with an `m.room_key.withheld` message carrying the
`m.blacklisted` code, and the state stays local to the device that set it.
