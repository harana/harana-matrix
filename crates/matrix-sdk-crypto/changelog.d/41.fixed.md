An `m.room.encrypted` event which was redacted before it could be decrypted is
now reported as such (`UnableToDecryptReason::RedactedEvent`) instead of as a
malformed event, so it no longer inflates the UTD metrics.
