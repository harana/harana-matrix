When several Olm sessions exist with a device, the one that most recently
decrypted a message successfully is now used to send the next message, as the
spec requires, instead of the most recently created one.
