A finished verification keeps the other side's device data:
`VerificationRequest::other_device_id` works in the done state, the new
`VerificationRequest::other_device_data` exposes the whole device, and
`VerificationRequestState::Done` carries it.
