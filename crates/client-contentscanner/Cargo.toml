[package]
authors = ["Jorge Martín Espinosa <jorgem@element.io>"]
description = "Optional crate used to allow scanning the contents of media to prevent malware infections"
name = "client-contentscanner"
version = "0.18.0"
edition = "2024"
license = "Apache-2.0"
rust-version.workspace = true

[features]
default = ["rustls-aws-lc-rs"]
e2e-encryption = []
uniffi = ["dep:uniffi", "client-matrix/uniffi"]

rustls-aws-lc-rs = ["client-matrix/rustls-aws-lc-rs"]

# Use ring rather than aws-lc-rs as the rustls crypto provider; see
# `matrix`'s feature of the same name.
rustls-ring = ["client-matrix/rustls-ring"]

[dependencies]
assert_matches2.workspace = true
client-matrix = { workspace = true, features = ["e2e-encryption", "testing"] }
client-crypto.workspace = true
common-ruma.workspace = true
serde.workspace = true
tracing.workspace = true
uniffi = { workspace = true, optional = true }

[dev-dependencies]
tokio = { workspace = true, features = ["macros"] }

[target.'cfg(not(target_family = "wasm"))'.dependencies]
common-test.workspace = true
wiremock.workspace = true

[lints]
workspace = true
