`NotificationProcessSetup::MultipleProcesses` now carries a `lock_holder_name`,
identifying the notification process in the cross-process lock guarding writes
to the SDK stores. It must be unique per process; previously it was hardcoded,
so two processes could collide on the same lock key.
