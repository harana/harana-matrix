`Room` gained `getStateEvent`, `getStateEvents`, `accountData` and
`setAccountData`. The bindings exposed no way to read arbitrary state events or
room account data, even though the SDK already held them; global account data
was already reachable through `Client::accountData`.
