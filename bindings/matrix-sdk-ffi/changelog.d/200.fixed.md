`Client::restore_session_with()` no longer resets the sliding sync version to
`None` when the restored session carries no version. A client built with native
sliding sync discovery could report the native version as available and still
fail to start its sync service with "sliding sync version is missing".
