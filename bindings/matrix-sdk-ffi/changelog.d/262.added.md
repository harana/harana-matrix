Expose log events to bindings consumers: `set_log_event_listener` registers a
`LogEventListener` that receives every log event passing the filter configured
in `init_platform`, carrying the level, target, message, source location and
timestamp. Delivery is implemented as an additional tracing layer, so the file
and system log writers keep working unchanged.
