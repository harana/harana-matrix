`RoomListEntriesDynamicFilterKind` gained a `Not` variant, which negates the
filters it contains. Clients can now exclude a category of rooms server-side of
the FFI boundary instead of filtering the entries themselves and still receiving
updates for the rooms they discard.
