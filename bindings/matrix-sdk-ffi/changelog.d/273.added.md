`Encryption::bootstrapCrossSigning` creates and publishes a cross-signing
identity for a client that authenticates first and offers a security setup step
afterwards. It reports a typed outcome (created, already present, authentication
required), never resets an existing identity, and returns no private key
material.
