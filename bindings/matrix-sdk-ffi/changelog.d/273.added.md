`Encryption::bootstrapCrossSigning` creates and publishes a cross-signing
identity after login, reporting whether it was created, was already present,
needs user-interactive authentication, or is unavailable. It never resets an
existing identity and returns no private key material.
