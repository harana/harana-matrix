`message_event_content_from_markdown` and
`message_event_content_from_markdown_as_emote` no longer turn a body that
carries no content beyond list markup, such as a bare `5.`, into an empty HTML
list; it is sent as plain text instead.
