`SessionVerificationControllerDelegate::did_finish` takes the details of the
device that was verified (user ID, device ID and display name), so a completion
dialog can name it. Implementations of the delegate need updating.
