`AuthDataPasswordDetails` gained a `session` field, carrying the session
identifier of the user-interactive authentication flow the data answers.
Without it, a second attempt at a flow the homeserver has already started is
rejected. Leave it unset for a first attempt.
