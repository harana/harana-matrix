Expose a panic hook over the FFI: `set_panic_listener` registers a
`PanicListener` that is called with the message, source location, thread name
and backtrace of any panic raised inside the SDK, so the host application can
offer a crash report or shut down cleanly. The hook chains to the panic
logging `init_platform` sets up rather than replacing it.
