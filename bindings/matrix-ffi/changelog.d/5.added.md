Add worked examples of driving the bindings under `bindings/examples`, in Swift
and in Kotlin. Each covers platform setup, building a client, password login
and session restore, cross-signing bootstrap, starting the sync service,
sending a message, and basic room operations. They are referenced from the
bindings README.
