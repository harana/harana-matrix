`ClientSessionDelegate::retrieve_session_from_keychain` and
`save_session_in_keychain` are async, which suits hosts whose keychain access
is asynchronous (React Native, for one), in particular around the OAuth
cross-process lock reload. Implementations of the delegate need updating.
