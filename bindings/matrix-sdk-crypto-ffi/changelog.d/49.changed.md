The `Logger` callback now receives the log level, the target, the message and
the structured fields as separate arguments instead of one flattened string, so
a downstream logger can route by severity and read the fields. Fields recorded
on the enclosing spans are passed along too.
