`VerificationRequest::other_device_display_name` returns the display name of
the device taking part in a verification, and `other_device_id` now keeps
returning it once the verification is done, so a completion dialog can name the
device that was verified.
