`get_element_call_required_permissions` is a thin wrapper over
`client_matrix::widget::Capabilities::element_call_required` rather than holding
the capability list itself. It returns a result now, because the user ID is
parsed: an invalid one is an error instead of producing state keys the widget
could never match.
