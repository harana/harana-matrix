`Encryption::bootstrapCrossSigning` republishes an identity whose keys were
created but never reached the homeserver, instead of reporting it as already
present. `CrossSigningStatus` carries the new `isPublished` flag.
