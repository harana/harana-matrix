`Encryption::waitForBackupUploadSteadyState` asks the upload task to run again.
Splitting the trigger out of `Backups::wait_for_steady_state()` left it waiting
for an upload nothing had started, so it only returned once the task woke for
some other reason.
